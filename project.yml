name: TikTik Downloader
options:
  bundleIdPrefix: com.nadet
  deploymentTarget:
    iOS: "16.0"
  xcodeVersion: "15"

settings:
  base:
    PRODUCT_NAME: TikTok Downloader
    MARKETING_VERSION: 1.0.0
    CURRENT_PROJECT_VERSION: 1
    SWIFT_VERSION: 5.9
    IPHONEOS_DEPLOYMENT_TARGET: 16.0
    TARGETED_DEVICE_FAMILY: 1  # iPhone only

targets:
  TikDown:
    type: application
    platform: iOS
    sources:
      - path: Sources
    info:
      path: Sources/Info.plist
      properties:
        CFBundleName: TikTok Downloader
        CFBundleDisplayName: TikTok Downloader
        CFBundleIdentifier: com.nadet.tiktokdownloader
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        UILaunchStoryboardName: ""
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSPhotoLibraryAddUsageDescription: "TikTok Downloader needs Photos access to save downloaded videos."
        LSApplicationQueriesSchemes:
          - tiktok
    settings:
      base:
        CODE_SIGNING_ALLOWED: NO
        ENABLE_BITCODE: NO
